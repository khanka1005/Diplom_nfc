"use client";

import React from "react";
import Template from "@/app/dashboard/template/template"; // Move Template to a separate component file

const Page = () => {
  return <Template />;
};

export default Page;
